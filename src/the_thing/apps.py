from django.apps import AppConfig

# pylint: disable = missing-class-docstring, invalid-name
class the_thingConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'the_thing'
